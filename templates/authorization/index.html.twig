{% extends 'base.html.twig' %}

{% block title %}Authorization index{% endblock %}

{% block body %}
    <h1>Authorization index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Role</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for authorization in authorizations %}
            <tr>
                <td>{{ authorization.id }}</td>
                <td>{{ authorization.role }}</td>
                <td>
                    <a href="{{ path('app_authorization_show', {'id': authorization.id}) }}">show</a>
                    <a href="{{ path('app_authorization_edit', {'id': authorization.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_authorization_new') }}">Create new</a>
{% endblock %}
