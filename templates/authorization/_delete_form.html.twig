<form method="post" action="{{ path('app_authorization_delete', {'id': authorization.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ authorization.id) }}">
    <button class="btn">Delete</button>
</form>
